CREATE database IF NOT EXISTS story CHARACTER SET UTF8;

# create table if not exists story.log1(
#     `id` INT(11) not null AUTO_INCREMENT primary key,
#     `memo` varchar(64),
#     `addTime` int(10)
# );
